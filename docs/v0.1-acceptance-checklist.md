# Kestrel v0.1 Acceptance Checklist

Maps every frozen architecture decision to its verification evidence.

| Decision                                   | Evidence                                                                                     |
| ------------------------------------------ | -------------------------------------------------------------------------------------------- |
| Dependency direction                       | `npm run boundaries` (scripts/check-boundaries.mjs)                                          |
| Domain purity                              | boundary scanner bans Node/Octokit/Ink/React/Commander/Execa/Zod/chalk in domain             |
| Mission lifecycle invariants               | src/domain/mission/mission.test.ts                                                           |
| Submission verification independence       | src/domain/mission/mission.test.ts                                                           |
| Additive evidence                          | src/domain/evidence/evidence-collection.test.ts                                              |
| Sidecar separation                         | src/infrastructure/workspace/filesystem-workspace-manager.test.ts                            |
| Atomic writes                              | src/infrastructure/fs/atomic-json-file.test.ts                                               |
| Single-writer locking                      | src/infrastructure/locking/file-mission-lock.test.ts                                         |
| Transaction recovery                       | src/application/transactions/commit-mission-change.test.ts                                   |
| Git/GitHub error classification            | src/infrastructure/git/git-error-mapper.ts, src/infrastructure/github/github-error-mapper.ts |
| Deterministic AgentBrief                   | src/application/agent/build-agent-brief.test.ts                                              |
| Plain/JSON output without ANSI             | src/cli/presentation/renderers.test.ts                                                       |
| Secret redaction                           | src/application/errors/kestrel-error.test.ts                                                 |
| Non-goals (no fork/push/PR/score/DB/cloud) | audit `rg` below                                                                             |

## Scope audit

- `rg` for forbidden scope found only two matches, both explicit non-goal documentation ("never computes XP", "no plugin registry"). No product-scope violations.
- `rg` for incomplete markers found `throw new Error` in two production locations (src/application/errors/kestrel-error.ts), which are constructor-input validation guards on a developer-facing value object — intentional. The remaining `throw new Error("expected ok")` occurrences are test-fixture assertion helpers in `*.test.ts` files only. No `TODO`, `TBD`, `FIXME`, `HACK`, `@ts-ignore`, or `eslint-disable` markers exist.

## Verification gate

Run from a clean generated-artifact state:

```
npm ci
npm run check
npm run build
npm test -- test/e2e test/package
npm pack --dry-run
```

The packed file list contains only `dist`, `README.md`, and `CHANGELOG.md` — no source fixtures, user state, tokens, or progress artifacts.
